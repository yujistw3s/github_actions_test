name: Check Environment Variables

on: [push]

jobs:
  check_variables:
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - name: Check Environment Variables
        run: |
          echo "Workflow Environment Variables:"
          env

          echo "Organization and Repository Environment Variables:"
          echo ${{ vars.dev.APP_ENV }}

